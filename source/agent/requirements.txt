# Bounds mirror pyproject.toml. The Dockerfile installs from THIS file, so the
# upper bounds must live here to keep the deployed container reproducible.
aws-opentelemetry-distro>=0.19.0,<1.0
bedrock-agentcore>=1.21.0,<2.0
botocore[crt]>=1.43.69,<2.0
mcp>=1.29.0,<2.0
strands-agents>=1.51.0,<2.0
PyYAML>=6.0,<7.0
defusedxml>=0.7.1,<1.0
